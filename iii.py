
def list_of_orders_total_prices ():
    get_list_price_String = int(input('''
    list of orders total prices of the current week in AUD
    format 100,33,744,...n
    '''))
    list_price = DOB.split(",")
    if len(DOB_list) < 3:
         print("Wrong Date of birth format...\nExiting signup...")
         exit()
    else:
         return date(int(DOB_list[0]), int(DOB_list[1]), int(DOB_list[2]))

class Sale:

    total_price_curr_week = cin_list()
    total_price_pre_week = cin_list()

    def __init__(self):
        return

